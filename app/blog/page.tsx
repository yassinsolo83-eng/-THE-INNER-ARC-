import Link from 'next/link'
import { SiteShell } from '@/components/site'
import { posts } from '@/lib/content'
export const metadata = { title: 'Journal — The Inner Arc', description: 'Essays and reflections on tarot, attention, and the questions we live with.' }
export default function Blog() { return <SiteShell><main className="mx-auto max-w-7xl px-6 py-24 lg:px-10 lg:py-32"><p className="text-xs uppercase tracking-[0.3em] text-accent">The journal</p><h1 className="mt-6 max-w-3xl font-serif text-6xl leading-none text-foreground md:text-8xl">Notes for the in-between.</h1><div className="mt-20 grid gap-6 md:grid-cols-3">{posts.map((post, index) => <article key={post.slug} className={`${index === 0 ? 'md:col-span-2 md:grid md:grid-cols-2' : ''} border border-border bg-card p-7`}><div className="flex min-h-48 items-end border border-border/70 bg-background p-5"><span className="font-serif text-5xl text-accent/80">{index === 0 ? 'I' : index === 1 ? 'II' : 'III'}</span></div><div className={index === 0 ? 'p-0 md:pl-8' : 'pt-7'}><p className="text-xs uppercase tracking-[0.18em] text-accent">{post.date} · {post.read}</p><h2 className="mt-4 font-serif text-3xl leading-tight text-foreground"><Link href={`/blog/${post.slug}`} className="hover:text-accent">{post.title}</Link></h2><p className="mt-4 text-sm leading-6 text-muted-foreground">{post.excerpt}</p><Link href={`/blog/${post.slug}`} className="mt-7 inline-flex text-sm text-accent">Read essay →</Link></div></article>)}</div></main></SiteShell> }
