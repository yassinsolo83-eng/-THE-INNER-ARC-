import Link from 'next/link'
import Image from 'next/image'
import { SiteShell } from '@/components/site'
import { posts } from '@/lib/content'
export const metadata = { title: 'Journal — The Inner Arc', description: 'Essays and reflections on tarot, attention, and the questions we live with.' }
export default function Blog() { return <SiteShell><main className="mx-auto max-w-7xl px-6 py-24 lg:px-10 lg:py-32"><p className="text-xs uppercase tracking-[0.3em] text-accent">The journal</p><h1 className="mt-6 max-w-3xl font-serif text-6xl leading-none text-foreground md:text-8xl">Notes for the in-between.</h1><div className="mt-20 grid gap-6 md:grid-cols-3">{posts.map((post, index) => <article key={post.slug} className={`${index === 0 ? 'md:col-span-2 md:grid md:grid-cols-2' : ''} group border border-border bg-card overflow-hidden`}><div className="relative min-h-48 overflow-hidden"><Image src={post.cover} alt={post.title} fill className="object-cover transition-transform duration-500 group-hover:scale-105" sizes={index === 0 ? '(max-width: 768px) 100vw, 50vw' : '(max-width: 768px) 100vw, 33vw'} /></div><div className="p-7"><p className="text-xs uppercase tracking-[0.18em] text-accent">{post.date} · {post.read}</p><h2 className="mt-4 font-serif text-3xl leading-tight text-foreground"><Link href={`/blog/${post.slug}`} className="hover:text-accent">{post.title}</Link></h2><p className="mt-4 text-sm leading-6 text-muted-foreground">{post.excerpt}</p><Link href={`/blog/${post.slug}`} className="mt-7 inline-flex text-sm text-accent">Read essay →</Link></div></article>)}</div></main></SiteShell> }
