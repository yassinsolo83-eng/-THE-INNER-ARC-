import { notFound } from 'next/navigation'
import Image from 'next/image'
import { ArrowLink, SiteShell } from '@/components/site'
import { posts } from '@/lib/content'
export function generateStaticParams() { return posts.map((post) => ({ slug: post.slug })) }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const post = posts.find((item) => item.slug === slug); return { title: post ? `${post.title} — The Inner Arc` : 'Journal — The Inner Arc', description: post?.excerpt } }
export default async function Article({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const post = posts.find((item) => item.slug === slug); if (!post) notFound(); return <SiteShell><main className="mx-auto max-w-7xl px-6 py-24 lg:px-10 lg:py-32"><div className="mx-auto max-w-3xl"><p className="text-xs uppercase tracking-[0.25em] text-accent">{post.date} · {post.read}</p><h1 className="mt-6 font-serif text-6xl leading-none text-foreground md:text-8xl">{post.title}</h1><p className="mt-8 text-xl leading-8 text-muted-foreground">{post.excerpt}</p><div className="relative mt-12 aspect-[16/9] w-full overflow-hidden rounded-sm"><Image src={post.cover} alt={post.title} fill className="object-cover" sizes="(max-width: 768px) 100vw, 768px" priority /></div><div className="mt-20 max-w-2xl space-y-8 text-base leading-8 text-muted-foreground"><p>There is a particular kind of knowing that arrives slowly. It does not announce itself; it gathers at the edge of attention, waiting for enough quiet to become legible.</p><p>Tarot can be a way of making that quiet intentional. The images give our thoughts somewhere to land, and the act of looking asks us to stay with what we might otherwise hurry past.</p><h2 className="pt-8 font-serif text-3xl text-foreground">A practice of noticing</h2><p>The point is not to hand our decisions over to a deck of cards. It is to meet our own material with a little more tenderness, curiosity, and room to move. A good question can change the shape of a day.</p><p>When you return to the cards, return to yourself. Let the symbol be a doorway rather than a verdict. The meaning is not fixed until you have had the chance to make it yours.</p><div className="border-t border-border pt-8"><ArrowLink href="/blog">Back to the journal</ArrowLink></div></div></div></main></SiteShell> }
