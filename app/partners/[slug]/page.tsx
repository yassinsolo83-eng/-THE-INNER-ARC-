import Link from 'next/link'
import { notFound } from 'next/navigation'
import { SectionHeading, SiteShell } from '@/components/site'
import { getPartner, partners } from '@/lib/partners'

export function generateStaticParams() { return partners.map((partner) => ({ slug: partner.slug })) }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const partner = getPartner((await params).slug); return { title: partner ? `${partner.name} — The Inner Arc` : 'Reader — The Inner Arc', description: partner?.specialty } }
export default async function PartnerProfile({ params }: { params: Promise<{ slug: string }> }) { const partner = getPartner((await params).slug); if (!partner) notFound(); return <SiteShell><main><section className="mx-auto max-w-7xl px-6 py-20 lg:px-10 lg:py-28"><Link href="/partners" className="text-sm text-accent">← Back to our readers</Link><div className="mt-14 grid gap-12 lg:grid-cols-[0.7fr_1.3fr] lg:items-end"><div className="aspect-[4/5] max-w-md overflow-hidden bg-muted"><img src={partner.photo} alt={`${partner.name}, tarot reader`} className="h-full w-full object-cover" /></div><div><p className="text-xs uppercase tracking-[0.24em] text-accent">{partner.yearsExperience} years reading</p><h1 className="mt-5 font-serif text-6xl leading-none text-foreground md:text-8xl">{partner.name}</h1><p className="mt-7 max-w-xl text-xl leading-8 text-muted-foreground">{partner.specialty}</p><div className="mt-7 flex flex-wrap gap-2">{partner.tags.map((tag) => <span key={tag} className="rounded-full border border-border px-4 py-2 text-xs text-muted-foreground">{tag}</span>)}</div></div></div><div className="mt-24 grid gap-16 lg:grid-cols-[1.1fr_0.9fr]"><div><SectionHeading eyebrow="A little context" title="The work, in their words."><p>{partner.bio}</p></SectionHeading><div className="mt-16 border-t border-border pt-10"><p className="text-xs uppercase tracking-[0.2em] text-accent">Approach to reading</p><p className="mt-5 max-w-2xl font-serif text-3xl leading-tight text-foreground">{partner.approach}</p></div></div><aside className="border border-border bg-card p-7"><p className="text-xs uppercase tracking-[0.2em] text-accent">Read with {partner.name.split(' ')[0]}</p><h2 className="mt-4 font-serif text-3xl text-foreground">Available formats</h2><ul className="mt-7 divide-y divide-border">{partner.readings.map((reading) => <li key={reading} className="py-4 text-sm text-muted-foreground">{reading}</li>)}</ul><Link href="#" className="mt-8 inline-flex w-full justify-center rounded-full bg-primary px-6 py-3 text-sm font-medium text-primary-foreground">Book a session with {partner.name.split(' ')[0]}</Link></aside></div><blockquote className="mx-auto mt-24 max-w-3xl border-t border-border pt-10 text-center"><p className="font-serif text-3xl leading-tight text-foreground">&ldquo;{partner.testimonial.quote}&rdquo;</p><cite className="mt-5 block text-xs not-italic uppercase tracking-[0.18em] text-accent">{partner.testimonial.name}</cite></blockquote></section></main></SiteShell> }
